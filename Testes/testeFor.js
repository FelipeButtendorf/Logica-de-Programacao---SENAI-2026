
let lerTeclado = require("readline-sync");
// for(let repetir = true; repetir ; repetir ) {
//     repetir = lerTeclado.keyInYN(`Gostaria de repetir o código?`)
// }
let num = null
for (num = lerTeclado.keyInYN(`Digite um número :`); num !== 0; num / 2 ) {
    if(num )
}